const form = document.getElementById("support-form");
const submitButton = document.getElementById("submit-button");
const messageBox = document.getElementById("form-message");
const API_URL = "https://suhaimi-support-api.bitsuhami.workers.dev/";
function showMessage(message, type, ticketUrl = "") { messageBox.innerHTML = ""; const text = document.createElement("div"); text.textContent = message; messageBox.appendChild(text); if (ticketUrl) { const link = document.createElement("a"); link.href = ticketUrl; link.textContent = "View your tickets"; link.className = "view-ticket-button"; messageBox.appendChild(link); } messageBox.className = `form-message ${type}`; messageBox.hidden = false; }
if (form) form.addEventListener("submit", async event => { event.preventDefault(); if (!form.checkValidity()) return form.reportValidity(); const data = new FormData(form); const payload = Object.fromEntries(data.entries()); submitButton.disabled = true; submitButton.textContent = "Submitting..."; try { const response = await fetch(API_URL, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); if (!response.ok) throw new Error(result.message || "Unable to submit the support request."); showMessage(`Support request submitted successfully. Ticket #${result.issueNumber}.`, "success", result.issueUrl); form.reset(); } catch (error) { showMessage(error.message, "error"); } finally { submitButton.disabled = false; submitButton.textContent = "Submit support request"; } });
